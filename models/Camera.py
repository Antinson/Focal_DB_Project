class Camera():
    def __init__(self, name: str, status: str, user_id: int, storage: str):
        self.name = name
        self.status = status
        self.user_id = user_id
        self.storage = storage